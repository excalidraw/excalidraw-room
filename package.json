{
  "dependencies": {
    "express": "4.17.1",
    "socket.io": "2.3.0",
    "typescript": "3.8.3"
  },
  "devDependencies": {
    "@types/express": "4.17.3",
    "@types/socket.io": "2.1.4"
  },
  "engines": {
    "node": "12.x.x"
  },
  "license": "MIT",
  "main": "index.js",
  "name": "excalidraw-io",
  "scripts": {
    "deploy": "yarn gcp-build && gcloud app deploy --project=excalidraw-io",
    "gcp-build": "tsc -p .",
    "prepare": "npm run gcp-build",
    "pretest": "npm run gcp-build",
    "start": "yarn gcp-build && node ./index.js"
  },
  "version": "1.0.0"
}
